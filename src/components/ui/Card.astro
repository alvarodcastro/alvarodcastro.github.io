---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = false } = Astro.props;
---

<div
  class={`bg-white rounded-xl border border-[#E5E7EB] p-6 ${hover ? 'hover:shadow-sm transition-shadow duration-150' : ''} ${className}`}
>
  <slot />
</div>
